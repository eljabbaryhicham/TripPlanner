"use client"

import * as React from "react"

// This component has been intentionally left blank to resolve a build issue
// caused by a dependency (`react-day-picker`) that is not compatible with React 19.
export function Calendar() {
  return null
}
